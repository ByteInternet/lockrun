#!/usr/bin/make -f

DPKG_EXPORT_BUILDFLAGS = 1
include /usr/share/dpkg/buildflags.mk

PREFIX = /usr
DESTDIR = debian/lockrun

%:
	dh $@

override_dh_auto_install:
	dh_auto_install $@ -- PREFIX=$(PREFIX) DESTDIR=$(DESTDIR)
